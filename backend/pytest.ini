[pytest]
# Run from the backend/ directory:
#   PYTHONPATH=..:. pytest tests/ -v
#
# Or from the project root:
#   PYTHONPATH=.:backend pytest backend/tests/ -v

testpaths = tests
addopts   = -v --tb=short
filterwarnings =
    ignore::UserWarning:app.core.security
    ignore::DeprecationWarning
