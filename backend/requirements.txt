# =================================================
# CDTool Backend — pinned dependencies
# Python 3.12+ required (Docker image uses 3.12-slim)
# Install: pip install -r requirements.txt
# Last updated: 2026-05-22
# =================================================

# --- Web framework ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9       # required for UploadFile / form data

# --- Database ---
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0
psycopg2-binary>=2.9.0        # PostgreSQL driver

# --- Configuration ---
pydantic-settings>=2.0.0,<3.0.0

# --- Authentication ---
PyJWT[crypto]>=2.8.0,<3.0.0   # JWT with RS256 (requires cryptography)
cryptography>=42.0.0           # RSA key generation / HMAC
passlib[bcrypt]>=1.7.4         # password hashing
bcrypt>=4.0.0
pyotp>=2.9.0                   # TOTP-based MFA

# --- Task queue ---
celery[redis]>=5.4.0,<6.0.0
redis>=5.0.0,<6.0.0

# --- Search / monitoring ---
elasticsearch>=8.15.0,<9.0.0

# --- Cloud storage ---
google-cloud-storage>=2.14.0,<3.0.0

# --- Validation engine (our own code) ---
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0
numpy>=1.26.0,<3.0

# --- HTTP client (tests + internal calls) ---
httpx>=0.27.0,<1.0.0

# --- Testing ---
pytest>=9.0.0
pytest-cov>=5.0.0

# --- Future ---
# reportlab>=4.0.0             # PDF generation
# weasyprint>=60.0             # HTML-to-PDF alternative
